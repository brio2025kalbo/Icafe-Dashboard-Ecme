# ─── API / Backend (internal app) ────────────────────────────────────────────
# Base URL of your own backend REST API (if you have one).
# In development the Vite dev server proxies /api/* requests to this URL.
# In production set this to the full URL of your deployed API server.
VITE_API_URL=http://localhost:3000

# Set to "false" to disable the axios-mock-adapter and hit the real backend.
# Set to "true" (or leave unset) to keep the built-in mock data while the real
# backend is not yet available.
VITE_ENABLE_MOCK=true

# ─── iCafe Cloud API ─────────────────────────────────────────────────────────
# Base URL of the iCafe Cloud REST API. The default value is correct for most
# setups — only change this if you use a regional / custom endpoint.
VITE_ICAFE_API_URL=https://api.icafecloud.com

# ── Single-café setup ──────────────────────────────────────────────────────
# Set these two variables when you only manage one café.
# The named exports in IcafeService.ts will use them automatically.

# Bearer API token.
# Generate this in your iCafe Cloud Admin Panel:
#   Settings → API settings → Generate API token
# Note: only one token is valid at a time; generating a new one revokes the old one.
# Note: the token is tied to the static IP(s) you whitelist in the Admin Panel.
VITE_ICAFE_API_KEY=

# Your Café ID — found in the iCafe Cloud dashboard URL or Settings page.
# All v2 API endpoints are scoped to this ID: /api/v2/cafe/{cafeId}/...
VITE_ICAFE_CAFE_ID=

# ── Multi-café setup ───────────────────────────────────────────────────────
# When you manage more than one café, set VITE_ICAFE_CAFES instead.
# This is a JSON array where each entry has cafeId, apiKey, and an optional
# human-readable name and apiUrl override.
# Leave VITE_ICAFE_API_KEY and VITE_ICAFE_CAFE_ID blank when using this.
#
# Example (all on one line in the actual .env file):
# VITE_ICAFE_CAFES=[{"cafeId":"111","apiKey":"tok_branch_a","name":"Branch A"},{"cafeId":"222","apiKey":"tok_branch_b","name":"Branch B"}]
VITE_ICAFE_CAFES=

# ─── Firebase (OAuth / optional) ─────────────────────────────────────────────
# Only required if you are using Firebase Authentication (e.g. Google sign-in).
# Leave these blank when using the standard email/password flow.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
